name: Docker CI with Gradle

on:
  push:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: gateway

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17

      - name: Execute Gradle build
        run: |
          chmod +x ../gradlew
          ../gradlew :gateway:bootJar

      - name: Archive build artifacts
        uses: actions/upload-artifact@v3
        with:
          name: gateway-artifact
          path: gateway/build/libs

      - name: Print contents of current directory
        run: ls -al
  
  build-image:

    runs-on: ubuntu-latest
    needs: build
    defaults:
      run:
        working-directory: gateway
    
    steps:
    
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Print contents of current directory
        run: ls -al
    
      - name: Download build artifacts
        uses: actions/download-artifact@v3
        with:
          name: gateway-artifact
          path: gateway/build/libs

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_PASSWORD }}

      - name: Build and push Docker image
        run: |
          docker build -t drprofessorowl/gateway:latest -f Dockerfile .
          docker push drprofessorowl/gateway:latest
